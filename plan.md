You are an expert full-stack developer.

Help me build a production-ready personal tech blog using Next.js and Markdown, and deploy it on Vercel.

Requirements:

* Use Next.js (latest version, App Router preferred)
* Blog content should be written in Markdown (.md files)
* Parse frontmatter (title, date, description, tags)
* Convert Markdown to HTML using remark or similar
* Create dynamic routes for blog posts (/posts/[slug])
* Homepage should list all blog posts with title, date, and short description
* Each blog page should render full content properly

UI/UX:

* Clean, minimal, modern design
* Mobile responsive
* Use Tailwind CSS for styling
* Add proper spacing and readable typography

Features:

* Syntax highlighting for code blocks
* SEO optimization (meta tags, Open Graph)
* Reading time estimation
* Tag/category support (optional but preferred)

Project Structure:

* /posts → Markdown files
* /lib → helper functions (markdown parsing)
* /app → routing (App Router)

Deployment:

* Push code to GitHub
* Deploy on Vercel
* Ensure zero-config deployment

Extras (if possible):

* Add search functionality
* Add comments (Giscus or similar)

Output:
* Step-by-step setup instructions
* Full code for each file
* Folder structure
* Commands to run
* Deployment guide

Keep explanations simple but precise.
Avoid unnecessary theory.
Focus on working implementation.
